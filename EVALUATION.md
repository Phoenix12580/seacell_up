# 重构方案评估：百万级单细胞 Metacell 全自适应极速构建系统

> 评估日期：2026-08-17 ｜ 评估对象：`seacell_up` 重构方案（分治 + 迭代清洗 + GNMF + 全自适应参数）

## 一、总体判断

**方案总体可行，架构方向正确，但存在 5 处技术硬伤 / 2 处工程缺口，直接照抄规范实现会失败或产生误导性结果。** 本仓库的实现保留了方案的全部核心设计（分治两阶段、NMF 曲率选维、GNMF、基尼/零值双指标、三分法搜 γ、硬容量约束），并在下列问题上做了必要修正。

**架构亮点（保留）：**

- **分治 + 回收**：按样本独立构建天然可并行（100 样本 ÷ 16 核 ≈ 7 波），且稀有细胞不会被大批量样本的批次效应在第一阶段吞没；回收池二次聚类正是稀有群体（如某样本中仅 20 个的浆细胞）能凑成一个 Metacell 的机会。
- **γ = Metacell 尺寸的单参数化**（K = N/γ） elegant，把"建多少个 Metacell"和"每个多大"统一成一个自由度。
- **无外部 R 依赖、纯 scipy.sparse 生态**（numpy / scipy / sklearn / anndata）。
- 质量评估完全内生（基尼 + 零值率），不需要 SEACells 依赖的 R 包。

## 二、问题清单与修正（按严重度排序）

### P1（数学不可行）：γ 搜索与固定容量区间 [25,35] 直接矛盾

规范同时要求：γ 在 [5,500] 内自适应搜索（即 Metacell 尺寸可在 5~500 间浮动），又要求 `enforce_capacity` 把每个 Metacell 严格限制在 [25,35]。当 γ_opt 偏离 30 时二者不可能同时满足——例如规范自己的 Step 3 测试：N=5000、K=100 → 平均桶容量 50，而 100 × 35 = 3500 < 5000，**按 [25,35] 约束 1500 个细胞无处可放，规格自相矛盾**。

**实现历史与最终决策**：初版采用容量界由数据推导（`target = N/K`，±20/25%）；Zhao 222K 首轮实测暴露副作用——第二阶段回收池（高异质 + 双指标尺寸偏置）被 γ 搜索推向 400+，产生 9 个装 7000-10000 细胞的巨型"Metacell"，违背 Metacell 语义。**2026-08-17 用户决策：每个 Metacell 严格 20-35 个细胞**。技术上通过把 K 钳制到容量可行域 `[⌈N/35⌉, ⌊N/20⌋]` 实现（`optimize_gamma(k_bounds=...)`），γ 搜索的可行域随之自动收敛到 [20,35]，任意样本规模下严格可行。`enforce_capacity` 仍支持任意显式区间（含可行性校验与报错），供高级用户覆盖。

### P2（评估指标有系统性偏置）：基尼系数随 K 单调漂移，三分法搜到的可能不是真最优

基尼系数对长度为 K 的向量计算——同样的分配质量，K 越大（行向量越长、尾部小值越多）基尼越高。因此 Score(γ) 含一个与质量无关、随 K 单调的分量。三分法前提是 Score(γ) 单峰，叠加此漂移后搜索可能被系统性带偏向大 K。

**修正**：无法在不偏离规范的前提下完全消除，采取三重缓解：(1) V 行归一化后按 argmax 一致性计算基尼（保留规范定义）；(2) 搜索阶段同一 K 只评估一次并缓存（相邻 γ 常映射同一 K，三分法实际评估次数 ≪ 6）；(3) `max_metacells` 资源上限防止大池子被漂移拉向巨 K。该偏置已在测试中量化观察，属已知局限（见 §四）。

### P3（数值稳定性）：MiniBatchNMF 随机性 → 重构误差曲线噪声 → 二阶差分选错 k

MiniBatchNMF 的小批量更新是随机的，e(k) 曲线的抖动幅度可能与相邻 k 间的真实差异同量级，二阶差分（本身就放大噪声）会选到伪曲率点。

**修正**：`init='nndsvda'`（确定性 SVD 初始化）+ 固定 `random_state` + 误差曲线 3 点滑动平均后再取曲率 argmax；NMF 选维阶段对超大数据行子采样（种子固定）。

### P4（规格缺失）：完全没有预处理环节 → 直接吃原始计数必错

NMF 要求非负输入且对计数深度敏感，方案从原始数据直接进 MiniBatchNMF，未提归一化、未提 HVG。更严重的是**跨阶段基因空间一致性**：第一阶段各样本独立选 HVG 的话，第二阶段合并回收细胞时不同样本的特征空间对不上。

**修正**：新增 `preprocessing.py`——入库时一次全局 log-normalize（CPM 1e4 + log1p）+ 全局 Seurat 式离散度 HVG（纯 numpy 实现分箱 z-score），所有阶段共用同一基因空间。注意"零经验参数"的诚实边界（见 §三）。

### P5（过剪风险）："Score 低于全局平均分即通过" 每轮回收约一半 Metacell

均值阈值是数据驱动的（符合"无经验阈值"字面要求），但对偏态分布意味着每轮约 30~50% 的 Metacell 被回收；叠加上限 3 轮迭代，最终噪音标记率可能高得不可接受（工业场景通常预期 <5%）。

**实测（固定容量 [20,35] 版）**：模拟数据噪音率 4.7%（其中真实边界细胞占大头）、pbmc3k 2.7%、Zhao 222K 11.0%（噪音细胞中 Epithelia 富集 2.99 倍、Immune/Stromal 类型占比 ≈0——即几乎只有肿瘤上皮连续谱细胞被剔，语义上正确；嫌高可用 `pass_rule="quantile"` + `pass_quantile=0.85` 放宽，或走上皮 CNV 分支）。

### P5b（实测新发现，已修）：自适应容量 + 双指标偏置 → 第二阶段巨型 Metacell

首轮 Zhao 222K 实测：回收池 82K 细胞被 γ 搜索收成 9 个 7000-10000 细胞的巨型 Metacell。根因是 ZeroPenalty 对大 Metacell 单调趋 0、小 K 的基尼基线偏高，双指标都偏向粗聚合（P2 的具体表现）。**修复 = 固定容量 [20,35] + K 可行域钳制**（见 P1 最终决策）。修复后 pbmc3k 噪音率 7.4%→2.7%，模拟数据稀有类型回收 96.5%→99.9%。

### E1（工程缺口）：第二阶段大池子的计算复杂度未评估

若第一阶段回收 10 万细胞，γ=30 时 K≈3333，GNMF 的 W 矩阵 100K×3333，单次乘法 ~66 GFLOP，6 次三分法搜索 × 全量迭代不可行。

**修正**：(1) 搜索阶段 GNMF 用较少迭代（默认 60）+ 对超搜索行数的池子做种子固定行子采样，γ 定格后在全量数据上做一次完整重拟合；(2) 乘性更新按结合律换序避开 N×K×K 平方项；(3) 图项滞后刷新；(4) 全程 float32。（16 核 / 873GB 实测无压力，见大测试结果。）

### E2（边界情况）：微样本（实测数据最小样本仅 4 个细胞）

K=int(N/γ)≥2 要求 N≥10；小于该数的样本无法形成任何 Metacell。

**修正**：细胞数不足的样本整块直接进回收池（`min_block_cells`），由第二阶段兜底，最终仍无法成团则标噪音。

## 三、"零经验参数"的诚实边界

规范的"严禁任何硬编码经验阈值"在算法核心（k_dim、γ、质量分、通过线、容量界）上**可以做到**——本实现这些量全部由数据推导。但以下配置参数无法从数据推出、只能作为显式文档化配置存在（非隐藏魔数）：`n_top_genes`（HVG 数，默认 2048）、kNN 图邻居数（10）、图正则权重 α（0.5）、max_iter、random_state。声称"零参数"是不诚实的，正确表述是"**零隐藏魔数：所有影响 Metacell 数量/尺寸/质量的参数均由数据自适应决定，其余为文档化的资源/数值配置**"。

## 四、验证（对应规范 Step 1–6 + PBMC 实测）

| 阶段 | 数据 | 结果 |
|---|---|---|
| Step 1–6 单测 | 模拟 2样本×5000细胞 | 全部通过：基尼速度 12ms/5ms、容量严格界、三分法收敛、全流水线无报错、异常 γ 跳过；**类型纯度中位 0.971、稀有类型(3%)回收 1380/1381 (99.9%)** |
| 小测试 | pbmc3k（2700×32738，raw，伪 2 样本） | 79 Metacells 全部 [20,35]，噪音 2.7%，557s；NK/T/B/Mono marker 干净富集 |
| 大测试 | Zhao_AllCells（222,529×20,055，93样本） | **6,000 Metacells 全部 [20,35]**，ct.main 纯度 mean 0.967/median 1.000（≥0.95 占 85.9%），ct.sub mean 0.938/median 1.000（≥0.95 占 75.6%），噪音 11.0%（几乎全为 Epithelia 肿瘤连续谱，富集 2.99x，其余类型≈0）；端到端 5,064s（84 min，16 核），峰值 29.9 GB |

自适应界版与固定界版对照（Zhao 222K）：ct.main ≥0.95 占比 65.6% → **85.9%**，ct.sub ≥0.95 占比 50.8% → **75.6%**，巨型 Metacell 消失，代价是噪音率 2.9% → 11.0%（可经 pass_rule 调节，或走 CNV 分支）与运行时间 26 → 84 分钟。

百万级外推（1.3M 细胞 / 100 样本 / 16 核）：第一阶段线性 ~30 分钟；第二阶段取决于回收率，按本测 47% 回收率外推约 2.5-4 小时，内存 <100 GB。

## 五、结论

| 维度 | 评价 |
|---|---|
| 架构（分治+回收+三层漏斗） | ✅ 合理，保留 |
| NMF 曲率选维 | ⚠️ 可行，需确定性初始化+平滑（已修） |
| GNMF + 容量约束 | ⚠️ 需自适应容量界（已修 P1） |
| 三分法搜 γ | ⚠️ 启发式可用，基尼 K-漂移是已知局限（P2） |
| 质量评估双指标 | ✅ 内生、快速；均值通过线需实测噪音率（P5） |
| 预处理 | ❌ 规格缺失，必须补（已补 P4） |
| 工程可行性 | ✅ 资源充足；大池子需子采样搜索（已修 E1） |

**建议：按修正后的方案实现。** 本仓库代码即按本评估的修正项落地，上皮/肿瘤细胞的 CNV 分支实验见 [docs/EXPERIMENTS.md](docs/EXPERIMENTS.md)。
